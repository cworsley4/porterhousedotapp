{
  "name": "porterhousedotapp",
  "version": "1.0.0",
  "description": "Website for the Porterhouse website",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "startdev": "PORT=3001 NODE_ENV=development node index.js",
    "watch": "PORT=3001 NODE_ENV=development node watch.js",
    "build": "node build.js",
    "builddev": "PORT=3001 NODE_ENV=development node build.js",
    "deploy": "NODE_ENV=production node build.js && gcloud app deploy --project porterhouse-223106",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Porterhouse",
    "messaging",
    "iMessage",
    "SMS",
    "mass",
    "messaging"
  ],
  "author": "Cecil Worsley IV",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "express-http-proxy": "^1.6.2",
    "http-proxy-middleware": "^1.1.1",
    "iframe-resizer": "^4.2.11",
    "ncp": "^2.0.0",
    "only": "0.0.2",
    "recursive-readdir": "^2.2.2",
    "serve": "^11.0.0"
  }
}
